{
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "prisma generate && next build",
  "regions": [
    "iad1"
  ],
  "crons": [
    {
      "path": "/api/swarm",
      "schedule": "*/5 * * * *"
    }
  ]
}